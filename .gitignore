node_modules
.next
out
next-env.d.ts
*.tsbuildinfo
.DS_Store

.vercel
.env*

# Snapshots crus da coleta: grandes e reproduziveis pelos scripts.
dados-brutos

# Permissoes de ferramenta por maquina, nao do projeto.
.claude/settings.local.json

# Skills de terceiros, tratadas como dependencia: o LOCK entra no
# repositorio, o conteudo nao. Sao 8 MB de markdown, fonte e CSV que
# nao foram escritos aqui e que `npx skills add` reconstroi a partir do
# skills-lock.json. `.claude/skills/` sao so symlinks para `.agents/`.
.agents/
.claude/skills/
